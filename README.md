gsc-flux-karma
==============

make sure bower, gulp, and karma are installed

`npm install gulp karma bower karma-cli -g`


install npm

`npm install`

install bower

`bower install`

run gulp

`gulp`

run karma

`karma start`
